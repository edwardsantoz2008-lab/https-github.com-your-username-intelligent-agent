const messages=document.querySelector('#messages'),form=document.querySelector('#form'),input=document.querySelector('#input'),typing=document.querySelector('#typing');
function add(role,text){const el=document.createElement('article');el.className=`message ${role}`;el.innerHTML=`<div class="avatar">${role==='assistant'?'◈':'YOU'}</div><div><label>${role==='assistant'?'MECH-AI':'OPERATOR'}</label><p></p></div>`;el.querySelector('p').textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight;return el}
function send(message){if(!message)return;add('user',message);input.value='';typing.textContent='PROCESSING...';form.querySelector('button').disabled=true;fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message)}).then(r=>r.json()).then(d=>add('assistant',d.response||d.detail||'No response received.')).catch(e=>add('assistant','Connection error: '+e.message)).finally(()=>{typing.textContent='READY FOR INPUT';form.querySelector('button').disabled=false;input.focus()})}
add('assistant','Systems online. I am MECH-AI, your local mechanical coding companion. Ask me a question, give me a coding task, or let me inspect your project.');
form.addEventListener('submit',e=>{e.preventDefault();send(input.value.trim())});document.querySelectorAll('[data-prompt]').forEach(b=>b.addEventListener('click',()=>send(b.dataset.prompt)));input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send(input.value.trim())}});
